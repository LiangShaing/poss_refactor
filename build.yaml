targets:
  $default:
    builders:
      # A SharedPartBuilder which uses the combining builder
      source_gen:combining_builder:
        options:
          build_extensions:
            '^lib/database_objects/realm/model/{{}}.dart': 'lib/database_objects/realm/model/generated/{{}}.g.dart'