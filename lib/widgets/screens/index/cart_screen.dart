import 'package:flutter/material.dart';
import 'package:mobile_poss_gp01/resources/size_style.dart';

class CartScreen extends StatelessWidget {
  const CartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(children: [
      /* 左邊區塊 */
      // const Expanded(child: CartLeftStatefulWidget()),
      //
      // /* 右邊區塊 */
      // Container(
      //     width: SizeStyle.fixedColumnWidth,
      //     margin: const EdgeInsets.all(SizeStyle.pagePadding),
      //     child: const CartRightStatefulWidget()),
    ]);
  }
}
